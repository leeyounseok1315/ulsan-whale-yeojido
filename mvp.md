# MVP 정의

## MVP 범위

### In Scope

- [x] 여지도(고지도) 톤 인터랙티브 지도와 고래 테마(행정구역 아님) 스팟 마커·범례
- [x] 핵심 5대 고래 스팟(고래박물관·생태체험관·문화마을·고래바다여행선·반구대 암각화) 단일 지도 통합 매핑·상세
- [x] TourAPI(areaCode=7) 전수 수집 + 데이터 정제·보정(공사 표기 sanitize 포함) + 이미지 정규화·프록시·폴백 + 고래 테마 태깅 + Redis 캐싱(키 버저닝·무효화) + 일 1회 야간 배치(실패 감지·헬스체크·알림) 파이프라인
- [x] 스팟 마커 클릭→상세 패널(이미지·설명·운영시간·휴무·요금·전화) detail* 연동
- [x] 동행 유형+체류 기간 입력 기반 코스 추천 엔진 v0(규칙 기반)
- [x] 추천 코스 결과 뷰(지도 경로 + 타임라인 카드, 실행 가능한 코스 단위)
- [x] 시즌·운영정보(고래바다여행선 4~10월 등) 현재 날짜(외부 주입 가능) 기반 추천·표시 직접 반영(비운항기 대체 포함)
- [x] 중립 데이터 출처('공공데이터') 표기 및 공사 명칭/로고 비노출 준수(절대규칙 #1)
- [x] 전역 에러 바운더리·404·오프라인/캐시 폴백 화면
- [x] 랜딩/온보딩(여지도 컨셉 스토리텔링) 및 모바일 우선 반응형 웹, Vercel 배포(안정 도메인·SSL)

### Out of Scope

- 네이티브 모바일 앱(iOS/Android)
- 영문/일문/중문 다국어(영문은 Stretch)
- 회원가입/로그인·경량 인증·서버 영속 후기(리뷰) CRUD 및 계정 연동 저장(Phase2/Stretch). 후기는 켤 경우에만 경량 인증·모더레이션 동반
- 주변 연계(음식점/카페/숙박)·현 위치 보강·검색 필터의 풀 구현 및 저장/즐겨찾기 서버 영속화(Phase2; 로컬 저장만 MVP)
- 초기 PostgreSQL/Prisma 등 DB 영속 레이어(영속 기능이 거의 없어 오버엔지니어링)
- 외부 예약/안내해설 신청 연계, AR/VR·암각화 가상 복원, 타지역(동해 고래 루트) 확장, ML 기반 추천·고도 이동 최적화
- 생태관광정보 서비스(GreenTourService) 지역기반 API 사용(절대규칙 #5)

## 핵심 사용자 흐름

1. 랜딩(여지도/고래의 시선 스토리) → 여지도 지도 진입 → 고래 테마 마커 탐색 → 마커 클릭 → 스팟 상세(이미지/운영시간/요금) 확인
2. 코스 추천 입력(동행 유형+체류 기간 선택) → 코스 추천 엔진 v0 호출 → 지도 경로+타임라인으로 추천 코스 확인 → 스팟 상세로 드릴다운
3. 현재 시즌 진입(또는 시간 모킹으로 비운항기 진입) → 고래바다여행선 비가용 배지 확인 → 대체 스팟 포함 코스 추천 수신
4. (Phase2) 지도에서 고래 테마/카테고리/이름 검색·필터로 스팟 좁히기 → 스팟 상세에서 주변 음식점/카페/숙박 연계 확인 → 관심 스팟/코스 저장(로컬, 서버 이관 가능 스키마)

## 성공 기준

- [ ] 사용자가 여지도 지도→고래 스팟→코스 추천을 한 흐름으로 끊김 없이 완료할 수 있다.
- [ ] 동행 유형·체류 기간 입력 시 운영시간/거리를 반영한 실행 가능한 코스가 생성되고 동행유형별로 결과가 차별화된다.
- [ ] 현재 날짜(외부 주입 가능) 기준 시즌(예: 고래바다여행선 운항 여부)이 추천/표시에 정확히 반영되며 비운항기 대체 스팟 코스가 시간 모킹으로 검증된다.
- [ ] 핵심 5대 고래 스팟이 정확한 좌표·상세정보·안정 이미지로 단일 지도에 표시된다.
- [ ] TourAPI 데이터가 정제·캐싱되어 첫 화면/지도 응답이 체감상 빠르다(캐시 적중, 런타임 직접 호출 최소화, 배치 실패 시 직전 스냅샷 폴백).
- [ ] 행정구역이 아닌 고래 테마 기반으로 콘텐츠가 조직되고 여지도 시각 차별점이 화면에서 즉시 드러난다.
- [ ] 사용자 노출 화면 어디에도 '한국관광공사/KTO' 명칭·로고가 없고 출처는 '공공데이터' 중립 표기로만 노출된다(절대규칙 #1).
- [ ] 모바일 화면에서 핵심 흐름이 정상 동작하고, 공개 배포 URL(안정 도메인·SSL)에서 심사위원 앞 라이브 데모가 캐시·이미지·전역 폴백 포함 오류 없이 재현된다.

## 권장 기술 스택 (모두 *조정 가능*, React/JavaScript 계열만 확정)

| 영역 | 권장값 | 비고 |
|---|---|---|
| 프론트엔드 | Next.js(App Router) + TypeScript + React, Tailwind CSS, TanStack Query | 세피아 고지도 톤 + 전역 폴백 화면. 순수 JS/Vite/SvelteKit/Vue 조정 가능 |
| 백엔드 | Next.js Route Handlers(BFF) + Node.js | TourAPI 프록시·캐싱·태깅·추천·이미지 프록시. serviceKey 서버 비밀. NestJS/Express 조정 가능 |
| 지도 | MapLibre GL JS 권장 | 국내 POI 우선 시 카카오맵 SDK. 라이선스·attribution·쿼터 W2 확정 |
| 캐시 | Upstash Redis | 키 버저닝·태그 퍼지·TTL·SWR·배치 실패 폴백. 자체 호스팅 조정 가능 |
| DB | MVP는 없음(Redis + 정적 JSON + localStorage) | Phase2/Stretch에서 Supabase/Vercel Postgres 검토 |
| 배포 | Vercel + Vercel Cron | GitHub CI·프리뷰/프로덕션 분리. Netlify·Cloudflare·GitHub Actions 조정 가능 |

---
본 문서는 자동 생성 초안이며 팀 상황에 맞게 조정하세요.